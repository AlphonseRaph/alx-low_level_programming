#include <stdio.h>

/**
 * main - addresses of variables
 *
 * Return: Always 0
 */

int main(void)
{
	char c;
	int n;

	printf("Addresses of variable 'c': %p\n", &c);
	printf("Addresses of variable 'n': %p\n", &n);
	return (0);
}
