#include "main.h"

/**
 * print_sign - Print sign of a number
 *
 * @n: number
 *
 * Return: 1 if n > 0, 0 if n = 0, -1 if n < 0)
 */

int print_sign(int n)
{
	if (n > 0)
		return (1);
	if (n = 0)
		return (0);
	if (n < 0)
		return (-1);
}
