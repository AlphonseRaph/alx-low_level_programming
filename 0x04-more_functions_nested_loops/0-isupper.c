#include "main.h"

/**
 * _isupper - Check for uppercase character
 *
 * @c: To be checked
 *
 * Return: 1 if uppercase, 0 otherwise
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	{
		return (0);
}
