#include<stdio.h>
#include<time.h>
#include<stdlib.h>

/**
 *
 * main - Print alphabet in lowercase
 *
 * Return - Always 0
 */
int main(void)
{
int ch;
for (ch = 'A'; ch <= 'Z'; ch++)
int tolower(int ch;)
put char (char);
return (0);
}
